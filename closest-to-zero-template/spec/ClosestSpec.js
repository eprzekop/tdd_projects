/*
	Find the closest number to zero in an input array.

	For instance, closest.find([8, -3, 2, -1, 13, 5]) should
	return -1.

	If two numbers are equally close to zero, the positive one
	should be considered "closer".

*/

describe("closest", function() {
    var closest;

    beforeEach(function() {
        closest = new Closest();
    });

    it("should do something", function() {
        expect(true).toEqual(false);
    });



});